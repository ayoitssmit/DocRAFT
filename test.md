### **Step 1: General Interface (No PDF Selected)**

1.  *Query*:
    ```text
    hello what can you do ?
    ```
    *   *(Shows instant conversational bypass with no citations).*

---

### **Step 2: Select `Raft Consensus Server Setup & Implementation Guide.pdf`**

2.  *Query*:
    ```text
    What are the five receiver rules for AppendEntries RPC in Raft?
    ```
    *   *(Shows precise HTML unescaping and math symbol rendering).*

3.  *Query*:
    ```text
    What does a follower do if the election timeout elapses without receiving heartbeats or granting a vote?
    ```
    *   *(Shows strict retrieval targeting without leaking context or rules from the history).*

4.  *Query*:
    ```text
    Write a thread-safe Python class that implements the persistent and volatile state variables for a Raft server, including a lock for synchronization.
    ```
    *   *(Shows complete code generation with zero placeholders, stubs, or pass statements).*

---

### **Step 3: Select `Affordable RAFT.pdf`**

5.  *Query*:
    ```text
    What is the name of the system proposed in this paper, and what shared task did it win first place in?
    ```
    *   *(Shows live ingestion and quick fact verification of Sherlock on the new document).*

6.  *Query*:
    ```text
    According to Section 3.3, what are the main caveats and limitations of using Retrieval Augmented Generation?
    ```
    *   *(Shows detailed semantic analysis of theoretical constraints from the research paper).*